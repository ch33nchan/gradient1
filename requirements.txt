# Core dependencies
# PyTorch CPU-only build (install separately, see below)
torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
pandas>=2.0.0
tqdm>=4.65.0
pyyaml>=6.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Environment
gym>=0.26.0

# Utilities
python-dotenv>=1.0.0
